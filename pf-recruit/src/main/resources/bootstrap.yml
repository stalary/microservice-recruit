spring:
  cloud:
    nacos:
      config:
        server-addr: nacos.stalary.com
        file-extension: yml
        shared-dataids: mysql.yml,printlog.yml,redis.yml
        refreshable-dataids: mysql.yml,redis.yml