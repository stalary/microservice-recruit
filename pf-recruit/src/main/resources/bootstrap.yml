spring:
  cloud:
    nacos:
      config:
        server-addr: nacos.stalary.com
        file-extension: yml
        shared-dataids: mysql.yml,printlog.yml,redis.yml,lightmq.yml
        refreshable-dataids: mysql.yml,redis.yml,lightmq.yml
  profiles:
    active: prod
  application:
    name: pf-recruit