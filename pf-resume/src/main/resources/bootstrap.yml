spring:
  cloud:
    nacos:
      config:
        server-addr: nacos.stalary.com
        file-extension: yml
        shared-dataids: redis.yml,mongo.yml,printlog.yml
        refreshable-dataids: redis.yml,mongo.yml
  profiles:
    active: prod
  application:
    name: pf-resume