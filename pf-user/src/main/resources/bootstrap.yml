spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        shared-dataids: mysql.yml,redis.yml,printlog.yml
        refreshable-dataids: mysql.yml,redis.yml