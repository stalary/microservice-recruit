spring:
  cloud:
    nacos:
      config:
        server-addr: nacos.stalary.com
        file-extension: yml
        shared-dataids: printlog.yml,lightmq.yml
        refreshable-dataids: lightmq.yml
  profiles:
    active: prod
  application:
    name: pf-consumer